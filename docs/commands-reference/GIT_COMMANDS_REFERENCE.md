# Git Commands Reference

## BASIC GIT COMMANDS

### git init
**รูปแบบการใช้งาน:**
```bash
git init [options] [directory]
```

**การใช้งาน:**
- สร้าง Git repository ใหม่ในโฟลเดอร์ปัจจุบัน

**Options ที่ใช้บ่อย:**
- `-b` (Branch) - กำหนดชื่อ branch หลัก (เช่น main, master)
- `--bare` - สร้าง bare repository (ไม่มี working directory)

**ตัวอย่างการใช้งาน:**
```bash
git init                            # สร้าง repo ในโฟลเดอร์ปัจจุบัน
git init myproject                  # สร้าง repo ในโฟลเดอร์ myproject
git init -b main                    # สร้าง repo โดยใช้ branch ชื่อ main
```

---

### git clone
**รูปแบบการใช้งาน:**
```bash
git clone [options] <repository-url> [directory]
```

**การใช้งาน:**
- คัดลอก repository จาก remote มาที่เครื่อง

**Options ที่ใช้บ่อย:**
- `-b` (Branch) - clone เฉพาะ branch ที่กำหนด
- `--depth` - clone เฉพาะ commit ล่าสุด (shallow clone)
- `--single-branch` - clone เฉพาะ branch เดียว

**ตัวอย่างการใช้งาน:**
```bash
git clone https://github.com/user/repo.git        # clone repository
git clone https://github.com/user/repo.git mydir  # clone ไปยังโฟลเดอร์ mydir
git clone -b develop https://github.com/user/repo.git  # clone branch develop
git clone --depth 1 https://github.com/user/repo.git   # shallow clone
```

---

### git status
**รูปแบบการใช้งาน:**
```bash
git status [options]
```

**การใช้งาน:**
- แสดงสถานะของ working directory และ staging area

**Options ที่ใช้บ่อย:**
- `-s` หรือ `--short` - แสดงแบบย่อ
- `-b` หรือ `--branch` - แสดงข้อมูล branch

**ตัวอย่างการใช้งาน:**
```bash
git status                          # แสดงสถานะแบบเต็ม
git status -s                       # แสดงสถานะแบบย่อ
git status -sb                      # แสดงแบบย่อพร้อม branch info
```

---

### git add
**รูปแบบการใช้งาน:**
```bash
git add [options] <file/directory>
```

**การใช้งาน:**
- เพิ่มไฟล์เข้า staging area เพื่อเตรียม commit

**Options ที่ใช้บ่อย:**
- `-A` หรือ `--all` - เพิ่มไฟล์ทั้งหมด (รวมลบ)
- `-u` หรือ `--update` - เพิ่มเฉพาะไฟล์ที่ถูกแก้ไข
- `-p` หรือ `--patch` - เลือก stage แบบ interactive

**ตัวอย่างการใช้งาน:**
```bash
git add file.txt                    # เพิ่มไฟล์เดียว
git add .                           # เพิ่มไฟล์ทั้งหมดในโฟลเดอร์ปัจจุบัน
git add -A                          # เพิ่มไฟล์ทั้งหมด (ทุกการเปลี่ยนแปลง)
git add *.js                        # เพิ่มไฟล์ .js ทั้งหมด
git add -p                          # เลือก stage แบบ interactive
```

---

### git commit
**รูปแบบการใช้งาน:**
```bash
git commit [options]
```

**การใช้งาน:**
- บันทึกการเปลี่ยนแปลงจาก staging area ลง repository

**Options ที่ใช้บ่อย:**
- `-m` (Message) - ระบุ commit message
- `-a` (All) - commit ไฟล์ที่ถูกแก้ไขทั้งหมด (ไม่รวมไฟล์ใหม่)
- `--amend` - แก้ไข commit ล่าสุด
- `-v` (Verbose) - แสดง diff ใน commit message editor

**ตัวอย่างการใช้งาน:**
```bash
git commit -m "Add new feature"     # commit พร้อม message
git commit -am "Update files"       # add + commit ไฟล์ที่แก้ไข
git commit --amend                  # แก้ไข commit ล่าสุด
git commit --amend -m "New message" # เปลี่ยน commit message ล่าสุด
```

---

### git push
**รูปแบบการใช้งาน:**
```bash
git push [options] [remote] [branch]
```

**การใช้งาน:**
- ส่ง commits จาก local repository ขึ้น remote repository

**Options ที่ใช้บ่อย:**
- `-u` หรือ `--set-upstream` - ตั้งค่า upstream branch
- `-f` หรือ `--force` - บังคับ push (ระวัง!)
- `--all` - push ทุก branches
- `--tags` - push tags ทั้งหมด

**ตัวอย่างการใช้งาน:**
```bash
git push                            # push ไปยัง upstream branch
git push origin main                # push ไปยัง branch main บน origin
git push -u origin feature          # push และตั้งค่า upstream
git push --all                      # push ทุก branches
git push --tags                     # push tags ทั้งหมด
```

**⚠️ คำเตือน:** `git push -f` จะเขียนทับ history บน remote ใช้ด้วยความระมัดระวัง!

---

### git pull
**รูปแบบการใช้งาน:**
```bash
git pull [options] [remote] [branch]
```

**การใช้งาน:**
- ดึง commits จาก remote repository มาที่ local และทำการ merge

**Options ที่ใช้บ่อย:**
- `--rebase` - ใช้ rebase แทน merge
- `--no-rebase` - ใช้ merge (default)
- `--ff-only` - ยอมรับเฉพาะ fast-forward merge

**ตัวอย่างการใช้งาน:**
```bash
git pull                            # pull จาก upstream branch
git pull origin main                # pull จาก branch main บน origin
git pull --rebase                   # pull และ rebase
git pull --ff-only                  # pull เฉพาะ fast-forward
```

---

### git fetch
**รูปแบบการใช้งาน:**
```bash
git fetch [options] [remote]
```

**การใช้งาน:**
- ดึงข้อมูลจาก remote repository โดยไม่ merge

**Options ที่ใช้บ่อย:**
- `--all` - fetch จากทุก remotes
- `--prune` - ลบ remote-tracking branches ที่ถูกลบบน remote
- `--tags` - fetch tags ทั้งหมด

**ตัวอย่างการใช้งาน:**
```bash
git fetch                           # fetch จาก remote ปัจจุบัน
git fetch origin                    # fetch จาก origin
git fetch --all                     # fetch จากทุก remotes
git fetch --prune                   # fetch และลบ branch ที่ไม่มีแล้ว
```

---

### git branch
**รูปแบบการใช้งาน:**
```bash
git branch [options] [branch-name]
```

**การใช้งาน:**
- แสดง, สร้าง, หรือลบ branches

**Options ที่ใช้บ่อย:**
- `-a` (All) - แสดงทุก branches (รวม remote)
- `-d` (Delete) - ลบ branch
- `-D` - บังคับลบ branch
- `-m` (Move/Rename) - เปลี่ยนชื่อ branch
- `-r` (Remote) - แสดงเฉพาะ remote branches

**ตัวอย่างการใช้งาน:**
```bash
git branch                          # แสดง local branches
git branch -a                       # แสดงทุก branches
git branch feature-x                # สร้าง branch ชื่อ feature-x
git branch -d feature-x             # ลบ branch feature-x
git branch -m old-name new-name     # เปลี่ยนชื่อ branch
```

---

### git checkout
**รูปแบบการใช้งาน:**
```bash
git checkout [options] <branch/commit>
```

**การใช้งาน:**
- สลับ branch หรือ restore ไฟล์

**Options ที่ใช้บ่อย:**
- `-b` - สร้าง branch ใหม่และสลับไปใช้
- `-B` - สร้างหรือ reset branch แล้วสลับไป
- `--` - ระบุว่าหลังจากนี้คือ filename

**ตัวอย่างการใช้งาน:**
```bash
git checkout main                   # สลับไปยัง branch main
git checkout -b feature-y           # สร้างและสลับไป branch ใหม่
git checkout -- file.txt            # restore file.txt จาก HEAD
git checkout abc123                 # สลับไปยัง commit abc123
```

**💡 หมายเหตุ:** Git เวอร์ชันใหม่แนะนำใช้ `git switch` และ `git restore` แทน

---

### git switch
**รูปแบบการใช้งาน:**
```bash
git switch [options] <branch>
```

**การใช้งาน:**
- สลับ branch (ทดแทน git checkout สำหรับการสลับ branch)

**Options ที่ใช้บ่อย:**
- `-c` (Create) - สร้าง branch ใหม่และสลับไปใช้
- `-C` - สร้างหรือ reset branch แล้วสลับไป
- `-d` (Detach) - สลับไปยัง detached HEAD state

**ตัวอย่างการใช้งาน:**
```bash
git switch main                     # สลับไปยัง branch main
git switch -c feature-z             # สร้างและสลับไป branch ใหม่
git switch -                        # สลับกลับไปยัง branch ก่อนหน้า
```

---

### git merge
**รูปแบบการใช้งาน:**
```bash
git merge [options] <branch>
```

**การใช้งาน:**
- รวม branch อื่นเข้ากับ branch ปัจจุบัน

**Options ที่ใช้บ่อย:**
- `--no-ff` - สร้าง merge commit เสมอ
- `--ff-only` - ยอมรับเฉพาะ fast-forward
- `--squash` - รวม commits เป็น commit เดียว
- `--abort` - ยกเลิก merge ที่มี conflict

**ตัวอย่างการใช้งาน:**
```bash
git merge feature-x                 # merge branch feature-x เข้ามา
git merge --no-ff feature-x         # merge โดยสร้าง merge commit
git merge --squash feature-x        # merge และรวม commits
git merge --abort                   # ยกเลิก merge ที่มี conflict
```

---

### git log
**รูปแบบการใช้งาน:**
```bash
git log [options]
```

**การใช้งาน:**
- แสดง commit history

**Options ที่ใช้บ่อย:**
- `--oneline` - แสดงแบบบรรทัดเดียวต่อ commit
- `-n` - กำหนดจำนวน commits ที่จะแสดง
- `--graph` - แสดงเป็น graph
- `--all` - แสดงทุก branches
- `--author` - กรองตาม author
- `--since` / `--until` - กรองตามวันที่

**ตัวอย่างการใช้งาน:**
```bash
git log                             # แสดง commit history
git log --oneline                   # แสดงแบบย่อ
git log --oneline --graph --all     # แสดงทุก branches แบบ graph
git log -5                          # แสดง 5 commits ล่าสุด
git log --author="John"             # แสดง commits ของ John
git log --since="2 weeks ago"       # แสดง commits 2 สัปดาห์ล่าสุด
```

---

### git diff
**รูปแบบการใช้งาน:**
```bash
git diff [options] [commit/branch]
```

**การใช้งาน:**
- แสดงความแตกต่างระหว่างไฟล์, commits, หรือ branches

**Options ที่ใช้บ่อย:**
- `--staged` หรือ `--cached` - แสดง diff ของ staged files
- `--stat` - แสดงสถิติการเปลี่ยนแปลง
- `--name-only` - แสดงเฉพาะชื่อไฟล์

**ตัวอย่างการใช้งาน:**
```bash
git diff                            # แสดง unstaged changes
git diff --staged                   # แสดง staged changes
git diff HEAD                       # แสดงทุกการเปลี่ยนแปลง
git diff branch1 branch2            # เปรียบเทียบ 2 branches
git diff commit1 commit2            # เปรียบเทียบ 2 commits
git diff --stat                     # แสดงสถิติการเปลี่ยนแปลง
```

---

### git reset
**รูปแบบการใช้งาน:**
```bash
git reset [options] [commit]
```

**การใช้งาน:**
- ย้าย HEAD ไปยัง commit ที่กำหนด และปรับ staging area หรือ working directory

**Options ที่ใช้บ่อย:**
- `--soft` - เก็บการเปลี่ยนแปลงใน staging area
- `--mixed` (default) - เก็บการเปลี่ยนแปลงใน working directory
- `--hard` - ลบการเปลี่ยนแปลงทั้งหมด (ระวัง!)

**ตัวอย่างการใช้งาน:**
```bash
git reset file.txt                  # unstage file.txt
git reset --soft HEAD~1             # ยกเลิก commit ล่าสุด (เก็บ staged)
git reset --mixed HEAD~1            # ยกเลิก commit ล่าสุด (เก็บ unstaged)
git reset --hard HEAD~1             # ยกเลิก commit ล่าสุดและลบการเปลี่ยนแปลง
git reset --hard origin/main        # reset ให้เหมือน origin/main
```

**⚠️ คำเตือน:** `git reset --hard` จะลบการเปลี่ยนแปลงถาวร ใช้ด้วยความระมัดระวัง!

---

### git stash
**รูปแบบการใช้งาน:**
```bash
git stash [options]
```

**การใช้งาน:**
- เก็บการเปลี่ยนแปลงชั่วคราวโดยไม่ต้อง commit

**คำสั่งย่อย:**
- `save` - เก็บการเปลี่ยนแปลง (ไม่จำเป็นใน Git เวอร์ชันใหม่)
- `list` - แสดง stash ทั้งหมด
- `pop` - นำ stash ล่าสุดกลับมาและลบ stash
- `apply` - นำ stash ล่าสุดกลับมาแต่ไม่ลบ stash
- `drop` - ลบ stash
- `clear` - ลบ stash ทั้งหมด

**ตัวอย่างการใช้งาน:**
```bash
git stash                           # เก็บการเปลี่ยนแปลง
git stash save "work in progress"   # เก็บพร้อม message
git stash list                      # แสดง stash ทั้งหมด
git stash pop                       # นำ stash ล่าสุดกลับมาและลบ
git stash apply                     # นำ stash ล่าสุดกลับมา
git stash drop stash@{0}            # ลบ stash ที่ระบุ
git stash clear                     # ลบ stash ทั้งหมด
```

---

### git remote
**รูปแบบการใช้งาน:**
```bash
git remote [options]
```

**การใช้งาน:**
- จัดการ remote repositories

**คำสั่งย่อย:**
- `add` - เพิ่ม remote
- `remove` หรือ `rm` - ลบ remote
- `rename` - เปลี่ยนชื่อ remote
- `-v` (Verbose) - แสดง URL ของ remotes

**ตัวอย่างการใช้งาน:**
```bash
git remote                          # แสดง remotes
git remote -v                       # แสดง remotes พร้อม URLs
git remote add origin <url>         # เพิ่ม remote ชื่อ origin
git remote remove origin            # ลบ remote origin
git remote rename origin upstream   # เปลี่ยนชื่อ remote
git remote get-url origin           # แสดง URL ของ origin
git remote set-url origin <new-url> # เปลี่ยน URL ของ origin
```

---

### git restore
**รูปแบบการใช้งาน:**
```bash
git restore [options] <file>
```

**การใช้งาน:**
- restore ไฟล์กลับไปสู่สถานะเดิม (ทดแทน git checkout สำหรับ restore)

**Options ที่ใช้บ่อย:**
- `--staged` - unstage ไฟล์
- `--source` - กำหนด source ที่จะ restore จาก
- `--worktree` - restore ใน working tree (default)

**ตัวอย่างการใช้งาน:**
```bash
git restore file.txt                # restore file.txt จาก HEAD
git restore --staged file.txt       # unstage file.txt
git restore --source=HEAD~1 file.txt # restore จาก commit ก่อนหน้า
git restore .                       # restore ทุกไฟล์
```

---

### git tag
**รูปแบบการใช้งาน:**
```bash
git tag [options] [tag-name]
```

**การใช้งาน:**
- สร้าง, แสดง, หรือลบ tags

**Options ที่ใช้บ่อย:**
- `-a` (Annotated) - สร้าง annotated tag
- `-m` (Message) - ระบุ tag message
- `-d` (Delete) - ลบ tag
- `-l` (List) - แสดง tags ตามรูปแบบ

**ตัวอย่างการใช้งาน:**
```bash
git tag                             # แสดง tags ทั้งหมด
git tag v1.0.0                      # สร้าง lightweight tag
git tag -a v1.0.0 -m "Version 1.0"  # สร้าง annotated tag
git tag -d v1.0.0                   # ลบ tag v1.0.0
git push origin v1.0.0              # push tag ไปยัง remote
git push origin --tags              # push tags ทั้งหมด
```