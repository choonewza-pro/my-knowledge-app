# Windows Commands Reference

## BASIC WINDOWS COMMANDS (CMD)

### dir
**รูปแบบการใช้งาน:**
```cmd
dir [path] [options]
```

**การใช้งาน:**
- แสดงรายการไฟล์และโฟลเดอร์

**Options ที่ใช้บ่อย:**
- `/A` - แสดงไฟล์ที่มี attributes เฉพาะ
- `/B` - แสดงเฉพาะชื่อไฟล์ (bare format)
- `/S` - แสดงไฟล์ในโฟลเดอร์ย่อยทั้งหมด
- `/P` - หยุดแสดงทีละหน้าจอ
- `/O` - เรียงลำดับ (N=name, S=size, D=date)
- `/W` - แสดงแบบกว้าง (wide format)

**ตัวอย่างการใช้งาน:**
```cmd
dir                                 :: แสดงรายการไฟล์
dir /A                              :: แสดงทุกไฟล์ (รวมซ่อน)
dir /B                              :: แสดงเฉพาะชื่อ
dir /S                              :: แสดงรวมโฟลเดอร์ย่อย
dir /O:N                            :: เรียงตามชื่อ
dir /O:S                            :: เรียงตามขนาด
dir /O:-D                           :: เรียงตามวันที่ (ใหม่สุดก่อน)
dir *.txt                           :: แสดงเฉพาะไฟล์ .txt
dir /A:D                            :: แสดงเฉพาะโฟลเดอร์
dir /A:H                            :: แสดงเฉพาะไฟล์ซ่อน
```

---

### cd (chdir)
**รูปแบบการใช้งาน:**
```cmd
cd [path]
chdir [path]
```

**การใช้งาน:**
- เปลี่ยน directory ปัจจุบัน

**ตัวอย่างการใช้งาน:**
```cmd
cd C:\Users\Username               :: ไปยัง path เฉพาะ
cd Documents                       :: ไปยังโฟลเดอร์ย่อย
cd ..                              :: ขึ้นไป 1 ระดับ
cd ..\..                           :: ขึ้นไป 2 ระดับ
cd \                               :: ไปยัง root ของ drive
cd /D D:\Projects                  :: เปลี่ยนทั้ง drive และ path
```

**💡 หมายเหตุ:** ใช้ `/D` เพื่อเปลี่ยนทั้ง drive

---

### md (mkdir)
**รูปแบบการใช้งาน:**
```cmd
md [drive:]path
mkdir [drive:]path
```

**การใช้งาน:**
- สร้างโฟลเดอร์ใหม่

**ตัวอย่างการใช้งาน:**
```cmd
md newfolder                       :: สร้างโฟลเดอร์
md folder1 folder2                 :: สร้างหลายโฟลเดอร์
md "My Documents"                  :: ชื่อมีช่องว่าง
md path\to\nested\folder           :: สร้างโฟลเดอร์หลายชั้น
```

---

### rd (rmdir)
**รูปแบบการใช้งาน:**
```cmd
rd [/S] [/Q] [drive:]path
rmdir [/S] [/Q] [drive:]path
```

**การใช้งาน:**
- ลบโฟลเดอร์

**Options ที่ใช้บ่อย:**
- `/S` - ลบโฟลเดอร์รวมไฟล์และโฟลเดอร์ย่อย
- `/Q` - ไม่ถามยืนยัน (quiet mode)

**ตัวอย่างการใช้งาน:**
```cmd
rd emptyfolder                     :: ลบโฟลเดอร์เปล่า
rd /S myfolder                     :: ลบโฟลเดอร์และไฟล์ภายใน
rd /S /Q temp                      :: ลบโดยไม่ถามยืนยัน
```

**⚠️ คำเตือน:** `rd /S /Q` จะลบทุกอย่างโดยไม่ถามยืนยัน!

---

### copy
**รูปแบบการใช้งาน:**
```cmd
copy [options] source destination
```

**การใช้งาน:**
- คัดลอกไฟล์

**Options ที่ใช้บ่อย:**
- `/Y` - ไม่ถามยืนยันเมื่อเขียนทับ
- `/V` - ตรวจสอบการคัดลอก
- `/A` - คัดลอก ASCII file
- `/B` - คัดลอก binary file

**ตัวอย่างการใช้งาน:**
```cmd
copy file.txt backup.txt           :: คัดลอกไฟล์
copy file.txt D:\Backup\           :: คัดลอกไปโฟลเดอร์อื่น
copy *.txt backup\                 :: คัดลอกทุกไฟล์ .txt
copy /Y file.txt backup.txt        :: เขียนทับโดยไม่ถาม
copy file1.txt+file2.txt combined.txt :: รวมไฟล์
```

---

### xcopy
**รูปแบบการใช้งาน:**
```cmd
xcopy source [destination] [options]
```

**การใช้งาน:**
- คัดลอกไฟล์และโฟลเดอร์ (ทำได้มากกว่า copy)

**Options ที่ใช้บ่อย:**
- `/E` - คัดลอกโฟลเดอร์รวมโฟลเดอร์ว่าง
- `/S` - คัดลอกโฟลเดอร์ (ไม่รวมโฟลเดอร์ว่าง)
- `/Y` - ไม่ถามยืนยัน
- `/I` - ถ้า destination ไม่มี ให้ถือว่าเป็นโฟลเดอร์
- `/H` - คัดลอกไฟล์ซ่อนและ system files
- `/D` - คัดลอกเฉพาะไฟล์ที่ใหม่กว่า
- `/V` - ตรวจสอบการคัดลอก

**ตัวอย่างการใช้งาน:**
```cmd
xcopy C:\Source D:\Backup /E       :: คัดลอกทุกอย่างรวมโฟลเดอร์ว่าง
xcopy C:\Source D:\Backup /S /Y    :: คัดลอกโดยไม่ถาม
xcopy C:\Source D:\Backup /E /H /Y :: คัดลอกทั้งหมดรวมไฟล์ซ่อน
xcopy *.doc D:\Backup /S           :: คัดลอกไฟล์ .doc ทั้งหมด
```

---

### robocopy
**รูปแบบการใช้งาน:**
```cmd
robocopy source destination [file] [options]
```

**การใช้งาน:**
- Robust File Copy - คัดลอกไฟล์แบบขั้นสูง (แนะนำสำหรับ backup)

**Options ที่ใช้บ่อย:**
- `/E` - คัดลอกโฟลเดอร์ย่อยรวมโฟลเดอร์ว่าง
- `/MIR` - Mirror directory tree (ลบไฟล์ที่ไม่มีใน source)
- `/MT[:n]` - Multi-threaded (n = จำนวน threads, default 8)
- `/R:n` - จำนวนครั้ง retry (default 1 million)
- `/W:n` - เวลารอระหว่าง retry (วินาที)
- `/LOG:file` - เขียน log ลงไฟล์
- `/NFL` - ไม่แสดงรายชื่อไฟล์
- `/NDL` - ไม่แสดงรายชื่อโฟลเดอร์
- `/NP` - ไม่แสดง progress
- `/XD dirs` - ไม่คัดลอกโฟลเดอร์ที่ระบุ
- `/XF files` - ไม่คัดลอกไฟล์ที่ระบุ

**ตัวอย่างการใช้งาน:**
```cmd
robocopy C:\Source D:\Backup /E    :: คัดลอกทั้งหมด
robocopy C:\Source D:\Backup /MIR  :: Mirror (ลบไฟล์เก่า)
robocopy C:\Source D:\Backup /E /MT:16 :: คัดลอกด้วย 16 threads
robocopy C:\Source D:\Backup /E /R:3 /W:5 :: retry 3 ครั้ง รอ 5 วินาที
robocopy C:\Source D:\Backup /E /LOG:backup.log :: เขียน log
robocopy C:\Source D:\Backup /E /XD node_modules :: ไม่คัดลอก node_modules
robocopy C:\Source D:\Backup /E /XF *.tmp :: ไม่คัดลอกไฟล์ .tmp
```

**💡 หมายเหตุ:** robocopy มี exit codes พิเศษ (0-7 = success, 8+ = error)

---

### move
**รูปแบบการใช้งาน:**
```cmd
move [/Y] source destination
```

**การใช้งาน:**
- ย้ายหรือเปลี่ยนชื่อไฟล์/โฟลเดอร์

**Options ที่ใช้บ่อย:**
- `/Y` - ไม่ถามยืนยันเมื่อเขียนทับ

**ตัวอย่างการใช้งาน:**
```cmd
move file.txt newname.txt          :: เปลี่ยนชื่อไฟล์
move file.txt D:\Archive\          :: ย้ายไฟล์
move *.txt D:\TextFiles\           :: ย้ายไฟล์ .txt ทั้งหมด
move /Y file.txt backup.txt        :: เขียนทับโดยไม่ถาม
move oldfolder newfolder           :: เปลี่ยนชื่อโฟลเดอร์
```

---

### del (erase)
**รูปแบบการใช้งาน:**
```cmd
del [options] [files]
erase [options] [files]
```

**การใช้งาน:**
- ลบไฟล์

**Options ที่ใช้บ่อย:**
- `/P` - ถามยืนยันก่อนลบแต่ละไฟล์
- `/F` - บังคับลบไฟล์ read-only
- `/S` - ลบไฟล์ในโฟลเดอร์ย่อยด้วย
- `/Q` - ไม่ถามยืนยัน (quiet mode)
- `/A` - ลบตาม attributes

**ตัวอย่างการใช้งาน:**
```cmd
del file.txt                       :: ลบไฟล์
del *.tmp                          :: ลบไฟล์ .tmp ทั้งหมด
del /P *.log                       :: ลบโดยถามยืนยัน
del /F /Q file.txt                 :: บังคับลบโดยไม่ถาม
del /S *.bak                       :: ลบไฟล์ .bak ในโฟลเดอร์ย่อยด้วย
```

**⚠️ คำเตือน:** ไฟล์ที่ลบด้วย `del` จะไม่ไป Recycle Bin!

---

### ren (rename)
**รูปแบบการใช้งาน:**
```cmd
ren [oldname] [newname]
rename [oldname] [newname]
```

**การใช้งาน:**
- เปลี่ยนชื่อไฟล์หรือโฟลเดอร์

**ตัวอย่างการใช้งาน:**
```cmd
ren file.txt newfile.txt           :: เปลี่ยนชื่อไฟล์
ren *.txt *.bak                    :: เปลี่ยน extension
ren oldfolder newfolder            :: เปลี่ยนชื่อโฟลเดอร์
```

---

### type
**รูปแบบการใช้งาน:**
```cmd
type [file]
```

**การใช้งาน:**
- แสดงเนื้อหาในไฟล์

**ตัวอย่างการใช้งาน:**
```cmd
type file.txt                      :: แสดงเนื้อหา
type file.txt | more               :: แสดงทีละหน้าจอ
type file1.txt file2.txt           :: แสดงหลายไฟล์
```

---

### more
**รูปแบบการใช้งาน:**
```cmd
more [file]
command | more
```

**การใช้งาน:**
- แสดงข้อความทีละหน้าจอ

**ตัวอย่างการใช้งาน:**
```cmd
more file.txt                      :: แสดงไฟล์ทีละหน้า
dir | more                         :: แสดงผลลัพธ์ทีละหน้า
type longfile.txt | more           :: แสดงทีละหน้า
```

---

### find
**รูปแบบการใช้งาน:**
```cmd
find [options] "string" [file]
```

**การใช้งาน:**
- ค้นหาข้อความในไฟล์

**Options ที่ใช้บ่อย:**
- `/I` - ไม่สนใจตัวพิมพ์เล็ก-ใหญ่
- `/N` - แสดงเลขบรรทัด
- `/C` - นับจำนวนบรรทัดที่พบ
- `/V` - แสดงบรรทัดที่ไม่มีข้อความ

**ตัวอย่างการใช้งาน:**
```cmd
find "error" log.txt               :: ค้นหา "error" ในไฟล์
find /I "warning" *.log            :: ค้นหาไม่สนตัวพิมพ์
find /N "error" log.txt            :: แสดงพร้อมเลขบรรทัด
find /C "error" log.txt            :: นับจำนวนบรรทัด
dir | find ".txt"                  :: กรองผลลัพธ์
```

---

### findstr
**รูปแบบการใช้งาน:**
```cmd
findstr [options] [strings] [file]
```

**การใช้งาน:**
- ค้นหาข้อความในไฟล์ (ทำได้มากกว่า find) รองรับ regex

**Options ที่ใช้บ่อย:**
- `/I` - ไม่สนใจตัวพิมพ์เล็ก-ใหญ่
- `/N` - แสดงเลขบรรทัด
- `/C:"string"` - ค้นหาตามตัวอักษรทุกตัว
- `/R` - ใช้ regular expressions
- `/S` - ค้นหาในโฟลเดอร์ย่อย
- `/M` - แสดงเฉพาะชื่อไฟล์
- `/V` - แสดงบรรทัดที่ไม่พบ

**ตัวอย่างการใช้งาน:**
```cmd
findstr "error" log.txt            :: ค้นหา error
findstr /I "error warning" *.log   :: ค้นหาหลายคำ
findstr /R "^error.*" log.txt      :: ใช้ regex
findstr /S "TODO" *.js             :: ค้นหาในโฟลเดอร์ย่อย
findstr /N /I "function" *.py      :: แสดงพร้อมเลขบรรทัด
```

---

### attrib
**รูปแบบการใช้งาน:**
```cmd
attrib [+R | -R] [+A | -A] [+S | -S] [+H | -H] [file]
```

**การใช้งาน:**
- แสดงหรือเปลี่ยน file attributes

**Attributes:**
- `R` - Read-only
- `A` - Archive
- `S` - System
- `H` - Hidden

**Options ที่ใช้บ่อย:**
- `/S` - ใช้กับไฟล์ในโฟลเดอร์ย่อย
- `/D` - ใช้กับโฟลเดอร์ด้วย

**ตัวอย่างการใช้งาน:**
```cmd
attrib file.txt                    :: แสดง attributes
attrib +R file.txt                 :: ตั้งเป็น read-only
attrib -R file.txt                 :: ยกเลิก read-only
attrib +H file.txt                 :: ซ่อนไฟล์
attrib -H -S file.txt              :: แสดงไฟล์และยกเลิก system
attrib +H *.txt /S                 :: ซ่อนไฟล์ .txt ทั้งหมด
attrib -H -S /S /D                 :: แสดงทุกอย่าง
```

---

### tree
**รูปแบบการใช้งาน:**
```cmd
tree [drive:][path] [options]
```

**การใช้งาน:**
- แสดง directory tree

**Options ที่ใช้บ่อย:**
- `/F` - แสดงชื่อไฟล์ด้วย
- `/A` - ใช้ ASCII characters

**ตัวอย่างการใช้งาน:**
```cmd
tree                               :: แสดง tree ของโฟลเดอร์ปัจจุบัน
tree /F                            :: แสดงพร้อมไฟล์
tree C:\Projects                   :: แสดง tree ของโฟลเดอร์เฉพาะ
tree /F > structure.txt            :: บันทึกลงไฟล์
```

---

### cls
**รูปแบบการใช้งาน:**
```cmd
cls
```

**การใช้งาน:**
- ล้างหน้าจอ Command Prompt

**ตัวอย่างการใช้งาน:**
```cmd
cls                                :: ล้างหน้าจอ
```

---

### echo
**รูปแบบการใช้งาน:**
```cmd
echo [message]
echo [ON | OFF]
```

**การใช้งาน:**
- แสดงข้อความหรือควบคุมการแสดงคำสั่ง

**ตัวอย่างการใช้งาน:**
```cmd
echo Hello World                   :: แสดงข้อความ
echo.                              :: แสดงบรรทัดว่าง
echo %DATE% %TIME%                 :: แสดงวันเวลา
echo Hello > file.txt              :: เขียนลงไฟล์ (overwrite)
echo World >> file.txt             :: เพิ่มต่อท้าย (append)
@echo off                          :: ปิดการแสดงคำสั่ง
```

---

### set
**รูปแบบการใช้งาน:**
```cmd
set [variable=[value]]
```

**การใช้งาน:**
- ตั้งค่า environment variables

**ตัวอย่างการใช้งาน:**
```cmd
set                                :: แสดง variables ทั้งหมด
set PATH                           :: แสดง PATH variable
set MYVAR=Hello                    :: ตั้งค่า variable
echo %MYVAR%                       :: แสดงค่า variable
set /A result=5+3                  :: คำนวณ (result=8)
set /P name="Enter name: "         :: รับ input จาก user
```

---

### setx
**รูปแบบการใช้งาน:**
```cmd
setx [variable] [value] [options]
```

**การใช้งาน:**
- ตั้งค่า environment variables แบบถาวร

**Options ที่ใช้บ่อย:**
- `/M` - ตั้งค่าระดับ system (ต้องเป็น admin)

**ตัวอย่างการใช้งาน:**
```cmd
setx JAVA_HOME "C:\Program Files\Java\jdk-17"
setx PATH "%PATH%;C:\NewPath"      :: เพิ่มใน PATH
setx /M SYSTEM_VAR "value"         :: ตั้งค่าระดับ system
```

**💡 หมายเหตุ:** ต้องเปิด Command Prompt ใหม่เพื่อให้มีผล

---

### path
**รูปแบบการใช้งาน:**
```cmd
path [[drive:]path[;...]]
```

**การใช้งาน:**
- แสดงหรือตั้งค่า PATH environment variable

**ตัวอย่างการใช้งาน:**
```cmd
path                               :: แสดง PATH ปัจจุบัน
path C:\Windows;C:\Windows\System32  :: ตั้งค่า PATH ใหม่
path %PATH%;C:\NewPath             :: เพิ่มใน PATH
```

---

## SYSTEM COMMANDS

### systeminfo
**รูปแบบการใช้งาน:**
```cmd
systeminfo [options]
```

**การใช้งาน:**
- แสดงข้อมูลระบบ

**ตัวอย่างการใช้งาน:**
```cmd
systeminfo                         :: แสดงข้อมูลระบบ
systeminfo | findstr /I "domain"   :: ค้นหาข้อมูล domain
systeminfo | findstr /C:"Total Physical Memory"  :: แสดง RAM
```

---

### tasklist
**รูปแบบการใช้งาน:**
```cmd
tasklist [options]
```

**การใช้งาน:**
- แสดงรายการ processes ที่กำลังทำงาน

**Options ที่ใช้บ่อย:**
- `/FI filter` - กรองตามเงื่อนไข
- `/V` - แสดงข้อมูลเพิ่มเติม
- `/SVC` - แสดง services

**ตัวอย่างการใช้งาน:**
```cmd
tasklist                           :: แสดง processes ทั้งหมด
tasklist /V                        :: แสดงรายละเอียด
tasklist | findstr chrome          :: ค้นหา Chrome
tasklist /FI "IMAGENAME eq chrome.exe"  :: กรอง Chrome
tasklist /FI "MEMUSAGE gt 100000"  :: process ใช้ RAM > 100 MB
tasklist /SVC                      :: แสดงพร้อม services
```

---

### taskkill
**รูปแบบการใช้งาน:**
```cmd
taskkill [options] [/PID process_id | /IM image_name]
```

**การใช้งาน:**
- ปิด process

**Options ที่ใช้บ่อย:**
- `/PID` - ระบุ Process ID
- `/IM` - ระบุชื่อโปรแกรม
- `/F` - บังคับปิด
- `/T` - ปิดรวม child processes

**ตัวอย่างการใช้งาน:**
```cmd
taskkill /PID 1234                 :: ปิด process ID 1234
taskkill /IM notepad.exe           :: ปิด Notepad
taskkill /F /IM chrome.exe         :: บังคับปิด Chrome
taskkill /F /IM chrome.exe /T      :: ปิด Chrome รวม tabs
taskkill /FI "MEMUSAGE gt 500000" /F  :: ปิด process ใช้ RAM > 500 MB
```

---

### shutdown
**รูปแบบการใช้งาน:**
```cmd
shutdown [options]
```

**การใช้งาน:**
- ปิด, restart, หรือ sleep เครื่อง

**Options ที่ใช้บ่อย:**
- `/s` - Shutdown
- `/r` - Restart
- `/h` - Hibernate
- `/l` - Logoff
- `/a` - ยกเลิก shutdown
- `/t seconds` - ตั้งเวลา (วินาที)
- `/f` - บังคับปิดโปรแกรม

**ตัวอย่างการใช้งาน:**
```cmd
shutdown /s                        :: ปิดเครื่อง
shutdown /r                        :: restart
shutdown /s /t 60                  :: ปิดเครื่องใน 60 วินาที
shutdown /r /t 0                   :: restart ทันที
shutdown /a                        :: ยกเลิก shutdown
shutdown /s /f /t 0                :: บังคับปิดเครื่องทันที
shutdown /h                        :: hibernate
shutdown /l                        :: logoff
```

---

### sfc
**รูปแบบการใช้งาน:**
```cmd
sfc [options]
```

**การใช้งาน:**
- System File Checker - ตรวจสอบและซ่อมแซมไฟล์ระบบ
- **ต้องรันในฐานะ Administrator**

**Options ที่ใช้บ่อย:**
- `/scannow` - สแกนและซ่อมแซมทันที
- `/verifyonly` - สแกนอย่างเดียว
- `/scanfile` - สแกนไฟล์เฉพาะ

**ตัวอย่างการใช้งาน:**
```cmd
sfc /scannow                       :: สแกนและซ่อมแซม
sfc /verifyonly                    :: สแกนอย่างเดียว
sfc /scanfile=C:\Windows\System32\kernel32.dll  :: สแกนไฟล์เฉพาะ
```

---

### chkdsk
**รูปแบบการใช้งาน:**
```cmd
chkdsk [drive:] [options]
```

**การใช้งาน:**
- Check Disk - ตรวจสอบและซ่อมแซมดิสก์
- **ต้องรันในฐานะ Administrator**

**Options ที่ใช้บ่อย:**
- `/F` - ซ่อมแซม errors
- `/R` - ซ่อมแซม bad sectors
- `/X` - force dismount ก่อนสแกน

**ตัวอย่างการใช้งาน:**
```cmd
chkdsk C:                          :: ตรวจสอบ drive C
chkdsk C: /F                       :: ตรวจสอบและซ่อมแซม
chkdsk C: /R                       :: สแกน bad sectors
chkdsk C: /F /R /X                 :: สแกนและซ่อมแซมทั้งหมด
```

**💡 หมายเหตุ:** อาจต้อง restart เครื่องเพื่อสแกน system drive

---

### diskpart
**รูปแบบการใช้งาน:**
```cmd
diskpart
```

**การใช้งาน:**
- จัดการ disk partitions
- **ต้องรันในฐานะ Administrator**

**คำสั่งภายใน diskpart:**
```cmd
list disk                          :: แสดงรายการ disks
list volume                        :: แสดงรายการ volumes
select disk 0                      :: เลือก disk 0
select volume 1                    :: เลือก volume 1
clean                              :: ลบ partitions ทั้งหมด
create partition primary           :: สร้าง partition
format fs=ntfs quick               :: format เป็น NTFS
assign letter=E                    :: กำหนด drive letter
exit                               :: ออกจาก diskpart
```

**⚠️ คำเตือน:** diskpart สามารถลบข้อมูลทั้งหมดได้ ใช้ด้วยความระมัดระวัง!

---

### format
**รูปแบบการใช้งาน:**
```cmd
format drive: [options]
```

**การใช้งาน:**
- Format disk
- **ต้องรันในฐานะ Administrator**

**Options ที่ใช้บ่อย:**
- `/FS:filesystem` - ระบุ file system (NTFS, FAT32, exFAT)
- `/Q` - Quick format
- `/V:label` - ตั้งชื่อ volume

**ตัวอย่างการใช้งาน:**
```cmd
format E:                          :: format drive E
format E: /FS:NTFS /Q              :: quick format เป็น NTFS
format E: /FS:FAT32 /Q /V:MyUSB    :: format FAT32 ชื่อ MyUSB
```

**⚠️ คำเตือน:** format จะลบข้อมูลทั้งหมด!

---

### wmic
**รูปแบบการใช้งาน:**
```cmd
wmic [class] [verb]
```

**การใช้งาน:**
- Windows Management Instrumentation Command - ข้อมูลระบบ

**ตัวอย่างการใช้งาน:**
```cmd
wmic cpu get name                  :: แสดงชื่อ CPU
wmic memorychip get capacity       :: แสดงขนาด RAM
wmic diskdrive get model,size      :: แสดงข้อมูล disk
wmic bios get serialnumber         :: แสดง serial number
wmic product get name              :: แสดงโปรแกรมที่ติดตั้ง
wmic os get caption,version        :: แสดง Windows version
wmic startup list brief            :: แสดงโปรแกรมตอน startup
```

**💡 หมายเหตุ:** wmic เลิกใช้แล้วใน Windows 10+, แนะนำใช้ PowerShell แทน

---

## NETWORK COMMANDS

### ipconfig
**รูปแบบการใช้งาน:**
```cmd
ipconfig [options]
```

**การใช้งาน:**
- แสดงข้อมูล network configuration

**Options ที่ใช้บ่อย:**
- `/all` - แสดงข้อมูลทั้งหมด
- `/release` - ปล่อย IP address
- `/renew` - ขอ IP address ใหม่
- `/flushdns` - ล้าง DNS cache
- `/displaydns` - แสดง DNS cache

**ตัวอย่างการใช้งาน:**
```cmd
ipconfig                           :: แสดงข้อมูลพื้นฐาน
ipconfig /all                      :: แสดงข้อมูลทั้งหมด
ipconfig /release                  :: ปล่อย IP
ipconfig /renew                    :: ขอ IP ใหม่
ipconfig /flushdns                 :: ล้าง DNS cache
ipconfig /displaydns               :: แสดง DNS cache
```

---

### ping
**รูปแบบการใช้งาน:**
```cmd
ping [options] hostname
```

**การใช้งาน:**
- ทดสอบการเชื่อมต่อเครือข่าย

**Options ที่ใช้บ่อย:**
- `-t` - ping จนกว่าจะกด Ctrl+C
- `-n count` - จำนวนครั้ง ping
- `-l size` - ขนาด packet (bytes)
- `-4` - บังคับใช้ IPv4
- `-6` - บังคับใช้ IPv6

**ตัวอย่างการใช้งาน:**
```cmd
ping google.com                    :: ping Google
ping -t 8.8.8.8                    :: ping ต่อเนื่อง
ping -n 10 google.com              :: ping 10 ครั้ง
ping -l 1000 google.com            :: packet ขนาด 1000 bytes
ping -4 google.com                 :: บังคับ IPv4
```

---

### tracert
**รูปแบบการใช้งาน:**
```cmd
tracert [options] hostname
```

**การใช้งาน:**
- ติดตามเส้นทาง network packets

**ตัวอย่างการใช้งาน:**
```cmd
tracert google.com                 :: ติดตามเส้นทางไป Google
tracert -h 30 google.com           :: กำหนด max hops = 30
tracert -d google.com              :: ไม่แปลง IP เป็นชื่อ
```

---

### netstat
**รูปแบบการใช้งาน:**
```cmd
netstat [options]
```

**การใช้งาน:**
- แสดงข้อมูล network connections

**Options ที่ใช้บ่อย:**
- `-a` - แสดงทุก connections
- `-n` - แสดง IP แทนชื่อ
- `-o` - แสดง PID
- `-b` - แสดงโปรแกรม (ต้องเป็น admin)
- `-r` - แสดง routing table

**ตัวอย่างการใช้งาน:**
```cmd
netstat                            :: แสดง connections
netstat -a                         :: แสดงทุก connections
netstat -n                         :: แสดง IP addresses
netstat -ano                       :: แสดงพร้อม PID
netstat -b                         :: แสดงโปรแกรม
netstat -r                         :: แสดง routing table
netstat -ano | findstr :80         :: หา connections บน port 80
```

---

### nslookup
**รูปแบบการใช้งาน:**
```cmd
nslookup [hostname] [dns-server]
```

**การใช้งาน:**
- ค้นหาข้อมูล DNS

**ตัวอย่างการใช้งาน:**
```cmd
nslookup google.com                :: ค้นหา IP ของ Google
nslookup google.com 8.8.8.8        :: ใช้ DNS server 8.8.8.8
nslookup -type=mx google.com       :: ค้นหา MX records
nslookup -type=ns google.com       :: ค้นหา nameservers
```

---

### netsh
**รูปแบบการใช้งาน:**
```cmd
netsh [context] [command]
```

**การใช้งาน:**
- Network Shell - จัดการ network settings

**ตัวอย่างการใช้งาน:**
```cmd
netsh interface show interface    :: แสดง network interfaces
netsh wlan show profiles           :: แสดง WiFi profiles
netsh wlan show profile name="WiFiName" key=clear  :: แสดง WiFi password
netsh wlan export profile folder=.  :: export WiFi profiles
netsh interface ip show config    :: แสดง IP configuration
netsh advfirewall show currentprofile  :: แสดงสถานะ firewall
netsh advfirewall set allprofiles state off  :: ปิด firewall
netsh advfirewall set allprofiles state on   :: เปิด firewall
```

---

### arp
**รูปแบบการใช้งาน:**
```cmd
arp [options]
```

**การใช้งาน:**
- แสดงและจัดการ ARP cache

**Options ที่ใช้บ่อย:**
- `-a` - แสดง ARP table
- `-d` - ลบ entry

**ตัวอย่างการใช้งาน:**
```cmd
arp -a                             :: แสดง ARP table
arp -d                             :: ล้าง ARP cache
```

---

### route
**รูปแบบการใช้งาน:**
```cmd
route [command] [options]
```

**การใช้งาน:**
- จัดการ routing table

**ตัวอย่างการใช้งาน:**
```cmd
route print                        :: แสดง routing table
route add 192.168.1.0 mask 255.255.255.0 192.168.0.1  :: เพิ่ม route
route delete 192.168.1.0           :: ลบ route
```

---

## BATCH FILE COMMANDS

### @echo off
**การใช้งาน:**
- ปิดการแสดงคำสั่งใน batch file

**ตัวอย่าง:**
```cmd
@echo off
echo This will be shown
:: คำสั่งจะไม่แสดง
```

---

### rem
**การใช้งาน:**
- Comment ใน batch file

**ตัวอย่าง:**
```cmd
rem This is a comment
:: This is also a comment (faster)
```

---

### pause
**การใช้งาน:**
- หยุดรอให้กดปุ่ม

**ตัวอย่าง:**
```cmd
@echo off
echo Processing...
pause
```

---

### goto
**การใช้งาน:**
- กระโดดไปยัง label

**ตัวอย่าง:**
```cmd
@echo off
goto end
echo This will not run
:end
echo Done
```

---

### if
**รูปแบบการใช้งาน:**
```cmd
if [condition] command
if [condition] (command1) else (command2)
```

**ตัวอย่าง:**
```cmd
if exist file.txt echo File exists
if not exist file.txt echo File not found

if "%1"=="" (
    echo No parameter
) else (
    echo Parameter: %1
)

if %errorlevel% equ 0 echo Success
```

---

### for
**รูปแบบการใช้งาน:**
```cmd
for %%variable in (set) do command
```

**ตัวอย่าง:**
```cmd
for %%f in (*.txt) do echo %%f
for /L %%i in (1,1,10) do echo %%i
for /D %%d in (*) do echo %%d
for /R %%f in (*.log) do del "%%f"
```

---

### call
**การใช้งาน:**
- เรียก batch file อื่นหรือ subroutine

**ตัวอย่าง:**
```cmd
call other.bat
call :subroutine
goto end

:subroutine
echo In subroutine
exit /b

:end
```

---

### exit
**การใช้งาน:**
- ออกจาก batch file หรือ Command Prompt

**ตัวอย่าง:**
```cmd
exit                               :: ออกจาก CMD
exit /b                            :: ออกจาก batch file
exit /b 1                          :: ออกด้วย error code 1
```

---

## USEFUL TIPS

### Redirection และ Piping
```cmd
command > file.txt                 :: เขียนลงไฟล์ (overwrite)
command >> file.txt                :: เพิ่มต่อท้าย (append)
command < file.txt                 :: อ่านจากไฟล์
command1 | command2                :: ส่งผลลัพธ์ไปคำสั่งถัดไป
command 2> error.txt               :: เขียน error ลงไฟล์
command > output.txt 2>&1          :: เขียนทั้ง output และ error
```

---

### Variables
```cmd
%variable%                         :: ใช้ตัวแปร
%1, %2, ...                        :: Parameters ของ batch file
%0                                 :: ชื่อ batch file
%*                                 :: Parameters ทั้งหมด
%CD%                               :: Current directory
%DATE%                             :: วันที่ปัจจุบัน
%TIME%                             :: เวลาปัจจุบัน
%RANDOM%                           :: ตัวเลขสุ่ม
%ERRORLEVEL%                       :: Exit code ของคำสั่งล่าสุด
%COMPUTERNAME%                     :: ชื่อเครื่อง
%USERNAME%                         :: ชื่อ user
%USERPROFILE%                      :: User profile path
%TEMP%                             :: Temp folder path
```

---

### Wildcards
```cmd
*                                  :: หลายตัวอักษร
?                                  :: ตัวอักษรเดียว

ตัวอย่าง:
*.txt                              :: ทุกไฟล์ .txt
file?.txt                          :: file1.txt, fileA.txt
test*.*                            :: test.txt, test.doc, testing.log
```

---

### Command Chaining
```cmd
command1 & command2                :: รันทั้งสองไม่ว่าจะสำเร็จหรือไม่
command1 && command2               :: รัน command2 ถ้า command1 สำเร็จ
command1 || command2               :: รัน command2 ถ้า command1 ล้มเหลว
```

---

### Keyboard Shortcuts
```
Ctrl + C                           :: ยกเลิกคำสั่ง
Ctrl + Break                       :: ยกเลิกคำสั่ง (แรงกว่า)
Tab                                :: Auto-complete ชื่อไฟล์/โฟลเดอร์
Up/Down Arrow                      :: เรียกคำสั่งก่อนหน้า
F7                                 :: แสดง command history
Ctrl + M                           :: เข้า Mark mode
Alt + Enter                        :: เปลี่ยนเป็น fullscreen
```

---

### Run as Administrator
```cmd
:: วิธีที่ 1: Right-click > Run as administrator
:: วิธีที่ 2: ใน Command Prompt
runas /user:Administrator cmd
```

---

### Useful Batch File Template
```cmd
@echo off
setlocal enabledelayedexpansion

:: ตรวจสอบ admin rights
net session >nul 2>&1
if %errorlevel% neq 0 (
    echo This script requires administrator privileges.
    pause
    exit /b 1
)

:: ตั้งค่า variables
set "SCRIPT_DIR=%~dp0"
set "LOG_FILE=%SCRIPT_DIR%script.log"

:: Main logic
echo Script started at %DATE% %TIME% >> "%LOG_FILE%"

:: Your commands here
echo Processing...

:: Error handling
if %errorlevel% neq 0 (
    echo Error occurred! >> "%LOG_FILE%"
    pause
    exit /b 1
)

echo Completed successfully!
echo Script finished at %DATE% %TIME% >> "%LOG_FILE%"
pause
exit /b 0
```

---

### Common Batch File Snippets

**ตรวจสอบไฟล์มีหรือไม่:**
```cmd
if exist "file.txt" (
    echo File exists
) else (
    echo File not found
)
```

**วนลูปไฟล์:**
```cmd
for %%f in (*.txt) do (
    echo Processing %%f
    type "%%f"
)
```

**สร้าง timestamp:**
```cmd
set timestamp=%DATE:~-4%%DATE:~3,2%%DATE:~0,2%_%TIME:~0,2%%TIME:~3,2%%TIME:~6,2%
set timestamp=%timestamp: =0%
echo %timestamp%
```

**ถามยืนยัน:**
```cmd
set /P confirm="Are you sure? (Y/N): "
if /I "%confirm%"=="Y" (
    echo Proceeding...
) else (
    echo Cancelled
)
```

**แสดง progress:**
```cmd
for /L %%i in (1,1,10) do (
    echo Processing %%i of 10
    timeout /t 1 /nobreak >nul
)
```
