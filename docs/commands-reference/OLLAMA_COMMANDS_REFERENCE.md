# Ollama Commands Reference

## BASIC OLLAMA COMMANDS

### ollama run
**รูปแบบการใช้งาน:**
```bash
ollama run [options] <model-name> [prompt]
```

**การใช้งาน:**
- รันโมเดลและเริ่มการสนทนาแบบ interactive
- สามารถส่ง prompt เริ่มต้นได้ทันที

**Options ที่ใช้บ่อย:**
- `--verbose` - แสดงข้อมูลเพิ่มเติมระหว่างรัน
- `--nowordwrap` - ปิดการตัดบรรทัดอัตโนมัติ

**ตัวอย่างการใช้งาน:**
```bash
ollama run llama2                   # รันโมเดล llama2
ollama run llama2 "สวัสดี"          # รันพร้อม prompt
ollama run codellama:7b             # รันเวอร์ชันเฉพาะ
ollama run llama2 --verbose         # รันพร้อมแสดงข้อมูลเพิ่มเติม
```

**💡 หมายเหตุ:** กด `Ctrl + D` หรือพิมพ์ `/bye` เพื่อออกจาก interactive mode

---

### ollama pull
**รูปแบบการใช้งาน:**
```bash
ollama pull <model-name>[:tag]
```

**การใช้งาน:**
- ดาวน์โหลดโมเดลจาก Ollama library

**ตัวอย่างการใช้งาน:**
```bash
ollama pull llama2                  # ดาวน์โหลดโมเดล llama2 (latest)
ollama pull llama2:13b              # ดาวน์โหลดเวอร์ชัน 13b
ollama pull codellama:latest        # ดาวน์โหลดเวอร์ชันล่าสุด
ollama pull mistral                 # ดาวน์โหลดโมเดล mistral
```

---

### ollama list
**รูปแบบการใช้งาน:**
```bash
ollama list
```

**การใช้งาน:**
- แสดงรายการโมเดลทั้งหมดที่ติดตั้งในเครื่อง
- แสดงข้อมูล: ชื่อโมเดล, ID, ขนาด, และเวลาที่แก้ไขล่าสุด

**ตัวอย่างการใช้งาน:**
```bash
ollama list                         # แสดงรายการโมเดลทั้งหมด
```

**ตัวอย่าง output:**
```
NAME              ID            SIZE      MODIFIED
llama2:latest     abc123...     3.8 GB    2 hours ago
codellama:7b      def456...     3.8 GB    1 day ago
mistral:latest    ghi789...     4.1 GB    3 days ago
```

---

### ollama rm
**รูปแบบการใช้งาน:**
```bash
ollama rm <model-name>[:tag]
```

**การใช้งาน:**
- ลบโมเดลออกจากเครื่อง

**ตัวอย่างการใช้งาน:**
```bash
ollama rm llama2                    # ลบโมเดล llama2
ollama rm llama2:13b                # ลบเวอร์ชัน 13b
ollama rm codellama:latest          # ลบเวอร์ชันล่าสุด
```

**⚠️ คำเตือน:** การลบโมเดลจะลบถาวร ต้องดาวน์โหลดใหม่หากต้องการใช้อีกครั้ง

---

### ollama create
**รูปแบบการใช้งาน:**
```bash
ollama create <model-name> -f <Modelfile>
```

**การใช้งาน:**
- สร้างโมเดลใหม่จาก Modelfile
- ใช้สำหรับ customize โมเดลที่มีอยู่

**Options ที่ใช้บ่อย:**
- `-f` หรือ `--file` - ระบุ path ของ Modelfile

**ตัวอย่างการใช้งาน:**
```bash
ollama create mymodel -f ./Modelfile           # สร้างโมเดลใหม่
ollama create custom-llama2 -f ./Modelfile     # สร้างโมเดล custom
```

**ตัวอย่าง Modelfile:**
```dockerfile
FROM llama2

# กำหนด system prompt
SYSTEM You are a helpful Thai language assistant.

# กำหนด temperature
PARAMETER temperature 0.8

# กำหนด context window
PARAMETER num_ctx 4096
```

---

### ollama show
**รูปแบบการใช้งาน:**
```bash
ollama show <model-name>[:tag]
```

**การใช้งาน:**
- แสดงข้อมูลรายละเอียดของโมเดล
- แสดง Modelfile, parameters, และ template

**Options ที่ใช้บ่อย:**
- `--modelfile` - แสดงเฉพาะ Modelfile

**ตัวอย่างการใช้งาน:**
```bash
ollama show llama2                  # แสดงข้อมูลโมเดล llama2
ollama show llama2:13b              # แสดงข้อมูลเวอร์ชัน 13b
ollama show llama2 --modelfile      # แสดงเฉพาะ Modelfile
```

---

### ollama cp
**รูปแบบการใช้งาน:**
```bash
ollama cp <source-model> <destination-model>
```

**การใช้งาน:**
- คัดลอกโมเดลเป็นชื่อใหม่
- ใช้สำหรับสร้าง backup หรือทดสอบการแก้ไข

**ตัวอย่างการใช้งาน:**
```bash
ollama cp llama2 llama2-backup      # คัดลอกโมเดล
ollama cp llama2:13b mymodel        # คัดลอกเป็นชื่อใหม่
```

---

### ollama push
**รูปแบบการใช้งาน:**
```bash
ollama push <model-name>[:tag]
```

**การใช้งาน:**
- อัพโหลดโมเดลไปยัง Ollama library (ต้องมี account)
- ใช้สำหรับแชร์โมเดล custom

**ตัวอย่างการใช้งาน:**
```bash
ollama push myusername/mymodel      # อัพโหลดโมเดล
ollama push myusername/llama2:custom # อัพโหลดเวอร์ชัน custom
```

**💡 หมายเหตุ:** ต้อง login ด้วย `ollama login` ก่อนใช้คำสั่งนี้

---

### ollama serve
**รูปแบบการใช้งาน:**
```bash
ollama serve
```

**การใช้งาน:**
- เริ่ม Ollama server
- รัน REST API server ที่ port 11434 (default)

**ตัวอย่างการใช้งาน:**
```bash
ollama serve                        # เริ่ม server
```

**💡 หมายเหตุ:** 
- Server จะรันที่ `http://localhost:11434`
- บน Windows/Mac, Ollama app จะรัน server อัตโนมัติ
- บน Linux, ต้องรัน `ollama serve` เอง

---

### ollama ps
**รูปแบบการใช้งาน:**
```bash
ollama ps
```

**การใช้งาน:**
- แสดงรายการโมเดลที่กำลังรันอยู่
- แสดงข้อมูล memory usage และเวลาที่โหลด

**ตัวอย่างการใช้งาน:**
```bash
ollama ps                           # แสดงโมเดลที่กำลังรัน
```

**ตัวอย่าง output:**
```
NAME              ID         SIZE      PROCESSOR    UNTIL
llama2:latest     abc123...  3.8 GB    100% GPU     4 minutes from now
```

---

## INTERACTIVE MODE COMMANDS

เมื่ออยู่ใน `ollama run` interactive mode สามารถใช้คำสั่งพิเศษได้:

### /bye
**การใช้งาน:**
- ออกจาก interactive mode

---

### /set
**รูปแบบการใช้งาน:**
```bash
/set parameter <value>
```

**การใช้งาน:**
- ปรับค่า parameter ระหว่างสนทนา

**Parameters ที่ใช้บ่อย:**
- `temperature` - ควบคุมความสุ่ม (0.0-1.0)
- `top_p` - nucleus sampling (0.0-1.0)
- `top_k` - จำนวน tokens ที่พิจารณา
- `num_ctx` - context window size

**ตัวอย่างการใช้งาน:**
```bash
/set temperature 0.8                # ตั้งค่า temperature
/set top_p 0.9                      # ตั้งค่า top_p
/set num_ctx 4096                   # ตั้งค่า context window
```

---

### /show
**รูปแบบการใช้งาน:**
```bash
/show <info|license|modelfile|parameters|system|template>
```

**การใช้งาน:**
- แสดงข้อมูลของโมเดลที่กำลังรัน

**ตัวอย่างการใช้งาน:**
```bash
/show info                          # แสดงข้อมูลทั่วไป
/show modelfile                     # แสดง Modelfile
/show parameters                    # แสดง parameters
/show system                        # แสดง system prompt
```

---

### /load
**รูปแบบการใช้งาน:**
```bash
/load <model-name>
```

**การใช้งาน:**
- โหลดโมเดลอื่นโดยไม่ต้องออกจาก interactive mode

**ตัวอย่างการใช้งาน:**
```bash
/load codellama                     # สลับไปใช้โมเดล codellama
/load llama2:13b                    # สลับไปใช้เวอร์ชัน 13b
```

---

### /clear
**การใช้งาน:**
- ล้าง conversation history
- เริ่มการสนทนาใหม่โดยไม่ต้องรีสตาร์ทโมเดล

**ตัวอย่างการใช้งาน:**
```bash
/clear                              # ล้าง conversation history
```

---

## ADVANCED USAGE

### Environment Variables
**ตัวแปรสภาพแวดล้อมที่ใช้บ่อย:**

```bash
# กำหนด host และ port
OLLAMA_HOST=0.0.0.0:11434

# กำหนดที่เก็บโมเดล
OLLAMA_MODELS=/path/to/models

# กำหนดจำนวน threads
OLLAMA_NUM_PARALLEL=4

# เปิด debug mode
OLLAMA_DEBUG=1
```

**ตัวอย่างการใช้งาน (Linux/Mac):**
```bash
export OLLAMA_HOST=0.0.0.0:11434
ollama serve
```

**ตัวอย่างการใช้งาน (Windows CMD):**
```cmd
set OLLAMA_HOST=0.0.0.0:11434
ollama serve
```

---

### REST API Usage

**การเรียกใช้ API:**

**Generate endpoint:**
```bash
curl http://localhost:11434/api/generate -d '{
  "model": "llama2",
  "prompt": "Why is the sky blue?",
  "stream": false
}'
```

**Chat endpoint:**
```bash
curl http://localhost:11434/api/chat -d '{
  "model": "llama2",
  "messages": [
    {
      "role": "user",
      "content": "Why is the sky blue?"
    }
  ],
  "stream": false
}'
```

**List models endpoint:**
```bash
curl http://localhost:11434/api/tags
```

**Show model info endpoint:**
```bash
curl http://localhost:11434/api/show -d '{
  "name": "llama2"
}'
```

---

## MODELFILE SYNTAX

### FROM
```dockerfile
FROM llama2
FROM llama2:13b
FROM ./path/to/model
```

### PARAMETER
```dockerfile
PARAMETER temperature 0.8
PARAMETER top_p 0.9
PARAMETER top_k 40
PARAMETER num_ctx 4096
PARAMETER stop "<|endoftext|>"
```

### SYSTEM
```dockerfile
SYSTEM """
You are a helpful assistant that speaks Thai.
Always respond in a polite and friendly manner.
"""
```

### TEMPLATE
```dockerfile
TEMPLATE """
{{ if .System }}<|system|>
{{ .System }}<|end|>
{{ end }}{{ if .Prompt }}<|user|>
{{ .Prompt }}<|end|>
{{ end }}<|assistant|>
{{ .Response }}<|end|>
"""
```

### ADAPTER
```dockerfile
ADAPTER ./path/to/adapter
```

### LICENSE
```dockerfile
LICENSE """
MIT License
...
"""
```

### MESSAGE
```dockerfile
MESSAGE user What is the capital of Thailand?
MESSAGE assistant The capital of Thailand is Bangkok.
```

---

## COMMON PARAMETERS

### temperature
- **ค่า:** 0.0 - 1.0
- **ค่าเริ่มต้น:** 0.8
- **คำอธิบาย:** ควบคุมความสุ่มของการตอบ (ต่ำ = แน่นอน, สูง = สร้างสรรค์)

### top_p
- **ค่า:** 0.0 - 1.0
- **ค่าเริ่มต้น:** 0.9
- **คำอธิบาย:** Nucleus sampling - พิจารณา tokens ที่มีความน่าจะเป็นรวมถึงค่านี้

### top_k
- **ค่า:** 1 - 100
- **ค่าเริ่มต้น:** 40
- **คำอธิบาย:** จำนวน tokens ที่พิจารณาในแต่ละขั้นตอน

### num_ctx
- **ค่า:** 128 - 8192
- **ค่าเริ่มต้น:** 2048
- **คำอธิบาย:** ขนาด context window (จำนวน tokens สูงสุดที่รับได้)

### num_predict
- **ค่า:** -1 (ไม่จำกัด) หรือ จำนวน tokens
- **ค่าเริ่มต้น:** 128
- **คำอธิบาย:** จำนวน tokens สูงสุดที่จะ generate

### repeat_penalty
- **ค่า:** 0.0 - 2.0
- **ค่าเริ่มต้น:** 1.1
- **คำอธิบาย:** ลงโทษการใช้คำซ้ำ (สูง = หลีกเลี่ยงการซ้ำมากขึ้น)

---

## TIPS & BEST PRACTICES

### การเลือกโมเดล
- **Small (3B-7B):** เหมาะสำหรับงานพื้นฐาน, รันเร็ว, ใช้ RAM น้อย
- **Medium (13B):** สมดุลระหว่างคุณภาพและความเร็ว
- **Large (70B+):** คุณภาพสูงสุด แต่ต้องการทรัพยากรมาก

### การ optimize performance
```bash
# ใช้ GPU ถ้ามี (automatic)
# ปรับจำนวน parallel requests
export OLLAMA_NUM_PARALLEL=2

# จำกัด context window ถ้าไม่จำเป็น
/set num_ctx 2048
```

### การจัดการ memory
```bash
# ดูโมเดลที่กำลังรัน
ollama ps

# ล้าง memory โดยปิดโมเดลที่ไม่ใช้
# โมเดลจะถูกปิดอัตโนมัติหลังไม่ใช้งาน 5 นาที
```

### การสร้าง custom model
```dockerfile
# Modelfile สำหรับ Thai chatbot
FROM llama2

SYSTEM """
คุณเป็นผู้ช่วยที่พูดภาษาไทยได้คล่อง
ตอบคำถามอย่างสุภาพและเป็นมิตร
ให้ข้อมูลที่ถูกต้องและเป็นประโยชน์
"""

PARAMETER temperature 0.7
PARAMETER top_p 0.9
PARAMETER num_ctx 4096
```

```bash
# สร้างโมเดล
ollama create thai-assistant -f ./Modelfile

# ทดสอบ
ollama run thai-assistant
```
