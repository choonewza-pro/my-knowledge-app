# คู่มือ GitHub — ความรู้พื้นฐานและกระบวนการทำงาน

## 1. GitHub คืออะไร

### ความหมาย
GitHub คือแพลตฟอร์มจัดเก็บซอร์สโค้ดบนคลาวด์ (Cloud) ที่ใช้ Git ในการจัดการเวอร์ชัน โดยเพิ่มความสามารถด้านการทำงานร่วมกันระหว่างนักพัฒนา

### เปรียบเทียบ Git vs GitHub
- **Git** = เครื่องมือจัดการเวอร์ชันที่ติดตั้งบนเครื่องคอมพิวเตอร์ (Local)
- **GitHub** = บริการออนไลน์ที่เก็บ Git repositories พร้อมเครื่องมือเสริม

### ฟีเจอร์หลักของ GitHub
- **Repositories** - พื้นที่เก็บโค้ด สามารถเป็น Public (เปิดให้ทุกคนดู) หรือ Private (เฉพาะทีม)
- **Pull Requests (PR)** - ขอให้ทีมตรวจสอบโค้ดก่อน merge เข้า branch หลัก
- **Issues** - ระบบติดตามงาน/บั๊ก เหมือนโน้ตแจ้งปัญหา
- **Actions** - ระบบ CI/CD อัตโนมัติ (เช่น auto test, auto deploy)
- **Projects** - จัดการงานแบบ Kanban board
- **Releases** - สร้างเวอร์ชันสำหรับปล่อยให้ผู้ใช้ดาวน์โหลด

### ประโยชน์ของการใช้ GitHub
✅ เก็บโค้ดปลอดภัย มี backup บนคลาวด์  
✅ ทำงานร่วมกับทีมได้ง่าย  
✅ ตรวจสอบประวัติการแก้ไขทุก commit  
✅ Code Review ผ่านระบบ Pull Request  
✅ สามารถ rollback กลับไปเวอร์ชันเก่าได้

## 2. การจัดการ Remote Repository

### Remote คืออะไร
Remote คือ repository ที่อยู่บนเซิร์ฟเวอร์ (เช่น GitHub) ไม่ใช่บนเครื่องเรา  
ปกติจะตั้งชื่อว่า `origin` (ชื่อมาตรฐาน)

### คำสั่งพื้นฐาน

#### 1. Clone (ดาวน์โหลด repo จาก GitHub มาเครื่องเรา)
```bash
git clone https://github.com/username/repo-name.git
```
**ใช้เมื่อ:** เริ่มทำงานโปรเจกต์ครั้งแรก หรือ ต้องการโค้ดของคนอื่น

#### 2. ตรวจสอบ Remote ที่เชื่อมต่ออยู่
```bash
git remote -v
```
**ผลลัพธ์ตัวอย่าง:**
```
origin  https://github.com/username/repo.git (fetch)
origin  https://github.com/username/repo.git (push)
```

#### 3. เพิ่ม Remote ใหม่
```bash
git remote add origin https://github.com/username/repo.git
```
**ใช้เมื่อ:** สร้าง repo ใหม่บนเครื่อง แล้วต้องการเชื่อมกับ GitHub

#### 4. Fetch (ดึงข้อมูลอัปเดตจาก GitHub แต่ยังไม่ merge)
```bash
git fetch origin
```
**ใช้เมื่อ:** ต้องการเช็คว่า GitHub มีอะไรใหม่ แต่ยังไม่พร้อม merge เข้าโค้ดเรา

#### 5. Pull (ดึงข้อมูลจาก GitHub และ merge ทันที)
```bash
git pull origin main
```
**ใช้เมื่อ:** ต้องการดึงโค้ดล่าสุดจาก GitHub มารวมกับโค้ดในเครื่อง  
**⚠️ หมายเหตุ:** Pull = Fetch + Merge รวมกัน

#### 6. Push (ส่งโค้ดจากเครื่องเราขึ้น GitHub)
```bash
git push -u origin main
```
**ใช้เมื่อ:** ทำงานเสร็จแล้ว ต้องการส่งโค้ดขึ้น GitHub  
**คำอธิบาย `-u`:** ตั้งค่าให้ครั้งต่อไปใช้แค่ `git push` ได้เลย

#### 7. ลบ Remote
```bash
git remote remove origin
```
**ใช้เมื่อ:** ต้องการยกเลิกการเชื่อมต่อกับ remote repository

### 🎯 สรุปการทำงาน Local ↔ Remote
```
Local Repository          GitHub (Remote)
     |                          |
     |------- git push -------→ |
     |                          |
     | ←------ git pull --------|
     |                          |
     | ←------ git fetch -------|
     | (ยังไม่ merge)             |
```

## 3. Workflow การทำงานคนเดียว

### กระบวนการพัฒนาฟีเจอร์ใหม่

#### ขั้นตอนที่ 1: เตรียม Repository
```bash
# สร้าง repo ใหม่บน GitHub แล้ว clone มา
git clone https://github.com/username/my-project.git
cd my-project

# หรือเชื่อม repo ที่มีอยู่แล้วกับ GitHub
git remote add origin https://github.com/username/my-project.git
git push -u origin main
```

#### ขั้นตอนที่ 2: สร้าง Feature Branch
```bash
git checkout -b feature/user-login
```
**เหตุผล:** แยกงานใหม่ออกจาก main เพื่อไม่ให้กระทบโค้ดหลัก

#### ขั้นตอนที่ 3: เขียนโค้ดและ Commit
```bash
# เขียนโค้ด...
git add .
git commit -m "เพิ่มระบบ login ด้วย JWT"

# เขียนโค้ดต่อ...
git add .
git commit -m "เพิ่มการตรวจสอบ email validation"
```

#### ขั้นตอนที่ 4: Push Branch ขึ้น GitHub
```bash
git push -u origin feature/user-login
```

#### ขั้นตอนที่ 5: สร้าง Pull Request (PR)
1. เข้า GitHub → เลือก repo
2. คลิก "Compare & pull request"
3. เขียนรายละเอียดว่าทำอะไรบ้าง
4. กด "Create pull request"

**ประโยชน์:** ตรวจสอบ code ก่อน merge, มีประวัติการทำงานชัดเจน

#### ขั้นตอนที่ 6: Merge เข้า Main
```bash
# บน GitHub: กด Merge pull request
# หรือบน Terminal:
git checkout main
git merge feature/user-login
git push origin main
```

#### ขั้นตอนที่ 7: Deploy ไป Production
```bash
git checkout production
git merge main
git push origin production
```

#### ขั้นตอนที่ 8: สร้าง Tag และ Release
```bash
git tag -a v1.0.0 -m "Release version 1.0.0 - เพิ่มระบบ login"
git push origin v1.0.0
```
**บน GitHub:** ไปที่ Releases → Draft a new release → เลือก tag v1.0.0 → Publish

---

### กระบวนการแก้ Bug (Hotfix)

#### เมื่อพบ Bug บน Production

**ขั้นตอนที่ 1:** สร้าง Hotfix Branch จาก production
```bash
git checkout production
git pull origin production
git checkout -b hotfix/fix-login-error
```

**ขั้นตอนที่ 2:** แก้ไข Bug
```bash
# แก้โค้ด...
git add .
git commit -m "แก้ไข bug การ login ไม่ได้เมื่อใช้ email ตัวพิมพ์ใหญ่"
```

**ขั้นตอนที่ 3:** Push และสร้าง PR ไป production
```bash
git push -u origin hotfix/fix-login-error
# สร้าง PR บน GitHub เพื่อ merge เข้า production
```

**ขั้นตอนที่ 4:** Merge เข้า Production และ Deploy
```bash
git checkout production
git merge hotfix/fix-login-error
git push origin production
```

**ขั้นตอนที่ 5:** Sync กลับไป Main (สำคัญ!)
```bash
git checkout main
git merge production
git push origin main
```
**เหตุผล:** ให้ main มีโค้ดล่าสุดเหมือน production

**ขั้นตอนที่ 6:** สร้าง Tag เวอร์ชันใหม่
```bash
git tag -a v1.0.1 -m "Hotfix: แก้ bug login"
git push origin v1.0.1
```

### 📊 ภาพรวม Branch Flow (คนเดียว)
```
main ─────────●────────●──────────●───→
              │        │          │
              │        │      (merge back)
              │        │          │
feature ──────●────●───┘          │
                                  │
production ───────────────●───────●───→
                          │
hotfix ───────────────────●──────→
                      (แก้ bug)
```  

## 4. Workflow การทำงานเป็นทีม

### บทบาทในทีม

#### 👑 Team Lead / Project Manager
- สร้าง repository บน GitHub
- ตั้งค่า branch protection rules
- ติดตั้ง CI/CD (GitHub Actions)
- อนุมัติ Pull Requests
- Deploy ไป production

#### 👨‍💻 Developer / Team Member
- Clone repository มาทำงาน
- สร้าง feature branch
- เขียนโค้ดและ push
- สร้าง Pull Request
- แก้ไขตาม code review

---

### ขั้นตอนการทำงานทีม

#### 1️⃣ Team Lead: เตรียม Repository
```bash
# สร้าง repo บน GitHub ชื่อ "team-project"
# ตั้งค่า branch protection:
# - ห้าม push ตรงไป main
# - ต้อง PR และได้รับ approval ก่อน merge
# - ต้อง pass CI tests

# สร้างไฟล์ .github/workflows/ci.yml สำหรับ CI/CD
```

#### 2️⃣ สมาชิกทีม: Clone และเริ่มทำงาน
```bash
git clone https://github.com/teamname/team-project.git
cd team-project
git checkout -b feature/add-payment-system
```

#### 3️⃣ สมาชิกทีม: เขียนโค้ด + Commit + Push
```bash
# เขียนโค้ด...
git add .
git commit -m "เพิ่มระบบชำระเงินผ่าน Stripe"

git push -u origin feature/add-payment-system
```

#### 4️⃣ สมาชิกทีม: สร้าง Pull Request
**บน GitHub:**
1. ไปที่ repository
2. กด "Compare & pull request"
3. เขียน Description:
   ```
   ## สิ่งที่เปลี่ยนแปลง
   - เพิ่ม payment API integration
   - เพิ่มหน้า checkout
   
   ## ทดสอบแล้ว
   - ✅ ทดสอบ payment ด้วย test card
   - ✅ ทดสอบ error handling
   ```
4. กำหนด Reviewers (Team Lead หรือเพื่อนร่วมทีม)
5. กด "Create pull request"

#### 5️⃣ Team Lead: Code Review
**ตรวจสอบใน PR:**
- ✅ โค้ดอ่านง่าย ตาม coding standards ไหม
- ✅ มี test cases ครบไหม
- ✅ CI tests ผ่านหมดไหม (เช่น unit tests, linting)
- ✅ มี security issues ไหม

**หากมีข้อสังเกต:**
```
# Comment ใน PR
"กรุณาเพิ่ม error handling สำหรับกรณี payment failed"
```

#### 6️⃣ สมาชิกทีม: แก้ไขตาม Review
```bash
# แก้โค้ดตามที่แนะนำ
git add .
git commit -m "เพิ่ม error handling สำหรับ payment failed"
git push origin feature/add-payment-system
```
**PR จะอัปเดตอัตโนมัติ!**

#### 7️⃣ Team Lead: Approve และ Merge
```
# บน GitHub PR:
1. กด "Approve"
2. กด "Merge pull request"
3. เลือก "Squash and merge" (รวม commits เป็น 1)
4. กด "Confirm merge"
```

#### 8️⃣ ทีมทั้งหมด: Pull โค้ดล่าสุด
```bash
git checkout main
git pull origin main
```
**ทำทุกครั้งก่อนสร้าง branch ใหม่!**

#### 9️⃣ Team Lead: Deploy ไป Production
```bash
git checkout production
git merge main
git push origin production

# สร้าง release tag
git tag -a v2.0.0 -m "Release v2.0.0 - เพิ่มระบบชำระเงิน"
git push origin v2.0.0
```

---

### 🔄 Daily Workflow สมาชิกทีม

**ทุกเช้าก่อนเริ่มงาน:**
```bash
git checkout main
git pull origin main
git checkout -b feature/new-task-name
```

**เมื่อทำงานเสร็จในแต่ละวัน:**
```bash
git add .
git commit -m "feat: เพิ่ม feature XYZ"
git push origin feature/new-task-name
```

**เมื่องานเสร็จสมบูรณ์:**
- สร้าง Pull Request บน GitHub
- รอ code review
- แก้ไขตาม feedback
- รอ merge จาก Team Lead

---

### 📋 Branch Protection Rules (แนะนำ)

**ตั้งค่าบน GitHub:**
- ✅ Require pull request before merging
- ✅ Require approvals (อย่างน้อย 1 คน)
- ✅ Require status checks to pass (CI tests)
- ✅ Require branches to be up to date
- ✅ Restrict who can push to matching branches

**ประโยชน์:**
- ป้องกันการ push โค้ดผิดพลาดเข้า main โดยตรง
- บังคับให้มี code review ก่อน merge
- รับประกันคุณภาพโค้ดด้วย automated tests

---

### 💡 Best Practices สำหรับทีม

#### การตั้งชื่อ Branch
```
feature/add-user-profile
bugfix/fix-login-redirect  
hotfix/critical-security-patch
refactor/update-api-structure
```

#### การเขียน Commit Message
```
feat: เพิ่มระบบ notification
fix: แก้ bug การคำนวณราคาผิด
docs: อัปเดต README
test: เพิ่ม unit tests สำหรับ payment
refactor: ปรับโครงสร้าง database schema
```

#### การสื่อสารในทีม
- 💬 Comment ใน PR อธิบายโค้ดที่ซับซ้อน
- 📝 เขียน description ใน PR ให้ชัดเจน
- 🔗 Link ไปยัง Issue ที่เกี่ยวข้อง (เช่น "Closes #123")
- ⏰ Review PR ภายใน 24 ชั่วโมง  

## 5. DevOps Basics

### DevOps คืออะไร

**DevOps** = **Dev**elopment + **Op**eration**s**  
เป็นแนวทางการทำงานที่รวมทีมพัฒนา (Dev) และทีมดูแลระบบ (Ops) เข้าด้วยกัน เพื่อให้การส่งมอบซอฟต์แวร์เร็วขึ้น น่าเชื่อถือ และอัตโนมัติ

### ปัญหาแบบเดิม (ก่อนมี DevOps)
❌ Dev เขียนโค้ดเสร็จ → ส่งให้ Ops → Ops deploy ไม่ได้ → โยนกลับ Dev  
❌ Deploy ใช้เวลานาน (สัปดาห์/เดือน)  
❌ Bug ค้นพบช้า เพราะ test ช้า  
❌ Manual deployment → ผิดพลาดง่าย

### วิธีแก้ปัญหาด้วย DevOps
✅ ทีม Dev และ Ops ทำงานร่วมกันตั้งแต่แรก  
✅ Automate ทุกอย่างที่ทำซ้ำได้ (testing, deployment)  
✅ Deploy บ่อย ๆ (วันละหลายครั้ง)  
✅ Monitor ระบบตลอดเวลา แก้ไขเร็ว

---

### องค์ประกอบหลักของ DevOps

#### 1️⃣ Collaboration (ทำงานร่วมกัน)
- ทีม Dev, QA, Ops นั่งประชุมร่วมกัน
- ใช้เครื่องมือเดียวกัน (เช่น GitHub, Slack, Jira)
- รับผิดชอบร่วมกัน (Dev ไม่โยนความผิดให้ Ops)

**ตัวอย่าง:**
```
Dev: "ผมเขียน code เสร็จแล้ว และทำ CI/CD pipeline ด้วย"
Ops: "เยี่ยม ผมช่วยเซ็ต monitoring และ alerting"
```

#### 2️⃣ Automation (ระบบอัตโนมัติ)
- **Auto Testing:** โค้ดทุก commit ถูก test อัตโนมัติ
- **Auto Deployment:** ผ่าน test แล้วก็ deploy เอง
- **Auto Scaling:** server เพิ่ม/ลดตาม traffic อัตโนมัติ

**ตัวอย่างเครื่องมือ:**
- GitHub Actions, Jenkins, CircleCI → CI/CD
- Terraform, Ansible → Infrastructure as Code
- Docker, Kubernetes → Container orchestration

#### 3️⃣ Continuous Integration (CI)
**คืออะไร:** นักพัฒนา push โค้ดบ่อย ๆ (วันละหลายครั้ง) และ merge รวมกันเป็นประจำ

**กระบวนการ CI:**
```
Developer push code
      ↓
Automated Tests (unit, integration)
      ↓
✅ Pass → Build และเตรียม deploy
❌ Fail → แจ้งเตือน developer ทันที
```

**ประโยชน์:**
- เจอ bug เร็ว แก้ง่าย
- ไม่มีปัญหา "ในเครื่องผมทำงานได้นะ" (works on my machine)

**ตัวอย่าง GitHub Actions CI:**
```yaml
name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm test
```

#### 4️⃣ Continuous Delivery (CD)
**คืออะไร:** โค้ดที่ผ่าน CI แล้วพร้อม deploy ได้ตลอดเวลา แต่ต้องกดปุ่ม deploy เอง

**Continuous Deployment (ต่างกันนิดเดียว):** deploy อัตโนมัติเลย ไม่ต้องกดปุ่ม!

**กระบวนการ CD:**
```
CI ผ่าน ✅
    ↓
Build Docker image
    ↓
Deploy to Staging (ทดสอบก่อน)
    ↓
🟢 กดปุ่ม → Deploy to Production
```

**ตัวอย่าง GitHub Actions CD:**
```yaml
name: CD
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm run build
      - run: ./deploy.sh production
```

#### 5️⃣ Monitoring & Feedback (ตลอดเวลา)
**คืออะไร:** ติดตามระบบหลัง deploy ว่าทำงานปกติไหม มี error ไหม

**เครื่องมือ:**
- **Logging:** เก็บ log ทุกอย่างที่เกิดขึ้น (ELK Stack, Datadog)
- **Monitoring:** ดูสถานะ server (CPU, RAM, Disk) (Prometheus, Grafana)
- **Alerting:** แจ้งเตือนเมื่อมีปัญหา (PagerDuty, Slack)

**ตัวอย่าง:**
```
🔴 Alert: API response time > 5 seconds
📊 CPU usage: 85%
📈 Request per minute: 10,000
```

---

### 🚀 DevOps Pipeline ทั้งหมด

```
Code → Build → Test → Deploy → Monitor
  ↓       ↓      ↓       ↓        ↓
 Git    Docker  CI   Production  Logs
             Jenkins/    K8s    Grafana
             Actions
```

**ตัวอย่างจริง:**
1. Developer push code → GitHub
2. GitHub Actions รัน tests อัตโนมัติ
3. ผ่าน tests → Build Docker image
4. Deploy ไป Staging server
5. QA ทดสอบบน Staging
6. กดปุ่ม Deploy → Production (Kubernetes)
7. Monitor ด้วย Grafana
8. มีปัญหา → Rollback ใน 30 วินาที

---

### 📊 เปรียบเทียบ: ก่อน vs หลัง DevOps

| หัวข้อ | ก่อน DevOps | หลัง DevOps |
|--------|-------------|-------------|
| **Deploy** | เดือนละ 1 ครั้ง | วันละหลายครั้ง |
| **Testing** | Manual ก่อน release | Automated ทุก commit |
| **Bug Detection** | หลัง deploy หลายวัน | ทันทีที่ push code |
| **Deployment Time** | หลายชั่วโมง | ไม่กี่นาที |
| **Rollback** | ยาก ใช้เวลานาน | กดปุ่มเดียว < 1 นาที |

---

### 💡 เริ่มต้น DevOps ง่าย ๆ (สำหรับมือใหม่)

#### Level 1: Basic CI
```yaml
# .github/workflows/test.yml
name: Run Tests
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm test
```

#### Level 2: CI + CD to Staging
```yaml
# เพิ่ม deployment step
- run: npm run build
- run: scp -r dist/ user@staging-server:/var/www/
```

#### Level 3: Full DevOps
- เพิ่ม Docker
- Deploy ไป Kubernetes
- เพิ่ม Monitoring (Prometheus)
- เพิ่ม Auto-scaling
- เพิ่ม Blue-Green Deployment

---

### 🎯 สรุป DevOps ง่าย ๆ

**DevOps = ทำให้การส่งมอบซอฟต์แวร์เร็ว ปลอดภัย และอัตโนมัติ**

**3 หลักสำคัญ:**
1. **Automate:** อะไรที่ทำซ้ำได้ → ทำให้อัตโนมัติ
2. **Collaborate:** Dev + Ops ทำงานร่วมกันตั้งแต่แรก
3. **Monitor:** ติดตามระบบตลอด แก้ไขเร็ว

**เริ่มต้นได้ทันที:**
- ใช้ GitHub Actions ทำ CI
- เขียน automated tests
- Monitor ด้วยเครื่องมือฟรี (Uptime Robot, Better Stack)  

