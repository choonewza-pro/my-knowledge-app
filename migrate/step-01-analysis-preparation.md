# Step 01 – การวิเคราะห์และเตรียมความพร้อม (Analysis & Preparation)

## 🎯 Goal

วิเคราะห์โครงสร้างของ static HTML website เดิม เข้าใจ features ทั้งหมด และเตรียมสภาพแวดล้อมสำหรับการพัฒนาโปรเจกต์ React + Vite + Tailwind CSS + daisyUI ให้พร้อมก่อนเริ่มต้น migration

---

## ✅ Tasks (Checklist)

### 1. วิเคราะห์โครงสร้างเว็บไซต์เดิม

- [ ] **สำรวจไฟล์ HTML ทั้งหมดใน `old-website/`**
  - ตรวจสอบจำนวนหน้าทั้งหมด (index, about, contact, product, map + knowledge pages)
  - บันทึก page title, meta description ของแต่ละหน้า
  - ระบุ sections หลักในแต่ละหน้า (hero, features, content, footer)

- [ ] **วิเคราะห์โครงสร้าง Navigation**
  - ศึกษา `partials/nav.html` เพื่อดู menu structure
  - จดบันทึก dropdown menus และ navigation hierarchy
  - ตรวจสอบ URL paths และ linking patterns

- [ ] **ตรวจสอบ CSS และ Styling**
  - เปิดดู `css/styles.css` (Bootstrap-based)
  - ระบุ custom styles ที่สำคัญ (`.hero-gradient`, `.knowledge-card`, `.feature-badge`, etc.)
  - หา inline styles ที่ต้องแปลงเป็น Tailwind classes

- [ ] **ตรวจสอบ JavaScript**
  - ดู `js/include-nav.js` และ `js/scripts.js`
  - วิเคราะห์ว่ามี interactive features อะไรบ้าง (dropdown, modal, smooth scroll)
  - ตัดสินใจว่าส่วนไหนควร migrate เป็น React components

- [ ] **ตรวจสอบ Assets**
  - นับจำนวนรูปภาพ, icons, fonts ใน `assets/`
  - ตรวจสอบ external dependencies (Bootstrap Icons, CDN links)
  - วางแผนการย้าย assets ไปยัง `public/` หรือ `src/assets/`

### 2. ศึกษา Dependencies ปัจจุบัน

- [ ] **อ่าน `package.json`**
  - ตรวจสอบ dependencies ที่ติดตั้งแล้ว:
    - React 19.2.0
    - react-router-dom 7.9.5
    - Tailwind CSS 4.1.17
    - daisyUI 5.5.3
  - ศึกษา scripts ที่มี (`dev`, `build`, `lint`, `preview`)

- [ ] **ศึกษา Vite Configuration**
  - เปิดดู `vite.config.ts`
  - ตรวจสอบ plugins ที่ใช้ (`@vitejs/plugin-react-swc`, `@tailwindcss/vite`)
  - เข้าใจ build settings และ output structure

- [ ] **ศึกษา TypeScript Configuration**
  - อ่าน `tsconfig.json` และ `tsconfig.app.json`
  - ตรวจสอบ compiler options (strict mode, path aliases)
  - เข้าใจ type checking rules

- [ ] **ศึกษา Tailwind CSS v4**
  - อ่านเอกสาร Tailwind CSS v4 เพื่อเข้าใจ breaking changes จาก v3
  - ตรวจสอบว่า `@import "tailwindcss"` ใช้งานอย่างไร
  - ศึกษา daisyUI v5.5.3 components และ themes

### 3. สร้าง Migration Checklist

- [ ] **สร้างตาราง Mapping**
  - จับคู่ HTML pages → React pages + routes
  - จับคู่ sections → React components
  - จับคู่ CSS classes → Tailwind utilities

- [ ] **กำหนด Component Hierarchy**
  - วาด component tree สำหรับแต่ละหน้า
  - ระบุ reusable components (Button, Card, Badge, etc.)
  - กำหนด layout components (Navbar, Footer, Layout)

- [ ] **วางแผน Routing Structure**
  - กำหนด route paths ทั้งหมด
  - ตัดสินใจว่าจะใช้ nested routes หรือไม่ (สำหรับ knowledge pages)
  - วางแผน lazy loading strategy

### 4. ตรวจสอบและเตรียม Development Environment

- [ ] **ตรวจสอบ Node.js และ npm/yarn**
  - ตรวจสอบ Node.js version (ควรเป็น v18+ หรือ v20+)
  - อัพเดต npm/yarn ให้เป็นเวอร์ชันล่าสุด

- [ ] **ติดตั้ง Dependencies (ถ้ายังไม่ได้ติดตั้ง)**
  ```powershell
  npm install
  ```

- [ ] **ทดลองรันโปรเจกต์**
  ```powershell
  npm run dev
  ```
  - ตรวจสอบว่า Vite dev server รันได้ปกติ
  - เปิดดู `http://localhost:5173` ใน browser

- [ ] **ทดสอบ Build**
  ```powershell
  npm run build
  ```
  - ตรวจสอบว่า build สำเร็จ
  - ตรวจสอบ output ใน `dist/` folder

### 5. Setup Version Control & Branching

- [ ] **สร้าง Git Branch สำหรับ Migration**
  ```powershell
  git checkout -b feature/migrate-from-static-html
  ```

- [ ] **Commit Baseline**
  - Commit โค้ดปัจจุบันก่อนเริ่ม migrate
  ```powershell
  git add .
  git commit -m "chore: baseline before migration from static HTML"
  ```

### 6. Setup Editor Configuration

- [ ] **ติดตั้ง VS Code Extensions (แนะนำ)**
  - ESLint
  - Prettier
  - Tailwind CSS IntelliSense
  - TypeScript + JavaScript
  - Auto Rename Tag

- [ ] **ตรวจสอบ ESLint Configuration**
  - อ่าน `eslint.config.js`
  - ตรวจสอบว่า lint rules ทำงานได้ถูกต้อง
  ```powershell
  npm run lint
  ```

### 7. สร้างเอกสาร Migration Log

- [ ] **สร้างไฟล์ `migrate/migration-log.md`**
  - บันทึกความคืบหน้าระหว่าง migration
  - จดปัญหาที่พบและแนวทางแก้ไข
  - เก็บ screenshots ของหน้าเดิมเพื่อเปรียบเทียบ

---

## 📝 Notes / Best Practices

### 1. วิเคราะห์ก่อนลงมือทำ

- **อย่าเพิ่งเขียนโค้ด**: ใช้เวลาทำความเข้าใจโครงสร้างเดิมให้ละเอียด
- **Screenshot หน้าเดิม**: เก็บภาพหน้าจอทุกหน้าเพื่อ reference
- **จดบันทึกรายละเอียด**: เขียน notes ลงใน migration-log.md เพื่อไม่ให้ลืม

### 2. เข้าใจ Modern Stack

- **Tailwind CSS v4 มี Breaking Changes**: เช่น `@tailwind base/components/utilities` เปลี่ยนเป็น `@import "tailwindcss"`
- **React Router v7 ใช้ Data Router API**: ต่างจาก v6, ต้องใช้ `createBrowserRouter()`
- **Vite vs Create React App**: Vite เร็วกว่า, ใช้ ESM, config ต่าง

### 3. Component-First Thinking

- **คิดเป็น Components**: HTML แต่ละส่วนคือ component ที่สามารถ reuse ได้
- **ออกแบบ Props Interface**: กำหนดว่า component ต้องการ props อะไรบ้าง
- **DRY (Don't Repeat Yourself)**: ถ้าโค้ดซ้ำ → แตก component ออกมา

### 4. Tailwind Migration Strategy

- **ไม่ต้องเก็บ CSS เดิม**: Bootstrap styles จะถูกแทนที่ด้วย Tailwind
- **ใช้ daisyUI Components**: แทนที่ Bootstrap components (Button, Card, Modal) ด้วย daisyUI
- **Custom Classes ใน @layer**: สร้าง custom utilities สำหรับ styles ซ้ำ ๆ

### 5. TypeScript Strict Mode

- **Enable strict**: ช่วยจับ bugs ตั้งแต่ compile time
- **Type ทุกอย่าง**: components, props, functions, data structures
- **ใช้ interfaces/types**: แทนการใช้ `any`

### 6. Performance Mindset

- **Code Splitting**: ใช้ `React.lazy()` สำหรับ pages
- **Optimize Assets**: ใช้ WebP สำหรับรูปภาพ, minify SVGs
- **Tree Shaking**: Vite ทำให้อัตโนมัติ, แต่ควร import เฉพาะที่ใช้

---

## ⚠️ สิ่งที่ควรระวัง

1. **อย่ารีบ**: Migration ที่ดีต้องใช้เวลา วิเคราะห์ให้ดีก่อน
2. **เก็บ old-website ไว้**: อย่าลบ, ใช้อ้างอิงตลอด
3. **Test บ่อย ๆ**: รัน `npm run dev` หลังจากทำทุก step
4. **Commit เป็นระยะ**: แบ่ง commit ตาม features/steps
5. **ถ้าสงสัย → อ่าน docs**: Tailwind, daisyUI, React Router มี docs ดีมาก

---

## ✨ Expected Outcome

หลังจากจบ Step 01 แล้ว ควรได้:

✅ ความเข้าใจชัดเจนเกี่ยวกับโครงสร้างเว็บไซต์เดิม  
✅ Mapping table ระหว่าง HTML → React components  
✅ Development environment พร้อมใช้งาน  
✅ Git branch สำหรับ migration  
✅ เอกสาร migration-log.md สำหรับบันทึกความคืบหน้า  
✅ ความมั่นใจที่จะเริ่ม Step 02 ได้ทันที

---

**หมายเหตุ**: Step นี้เป็น foundation ของทั้งโปรเจกต์ ใช้เวลาให้คุ้มค่า อย่ารีบก้าวต่อไปถ้ายังไม่มั่นใจ
